{
  "name": "ShortcutIcon",
  "type": "module",
  "version": "1.0.1",
  "private": true,
  "description": "タブに表示されるアイコンを設定します。（ブラウザに保存されます）",
  "scripts": {
    "dev": "vite",
    "build:app": "vite build",
    "build:plugin": "mt-plugin-builder build",
    "build": "npm run build:app && npm run build:plugin"
  },
  "devDependencies": {
    "@usualoma/mt-plugin-builder": "^1.0.0",
    "vite": "^5.4.11"
  }
}
